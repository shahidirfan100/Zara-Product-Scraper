{
    "title": "Zara Product Scraper",
    "description": "Extract product listings from Zara category pages. Get product details including prices, names, images, and availability.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Zara category URL to scrape products from.",
            "editor": "textfield",
            "prefill": "https://www.zara.com/uk/en/woman-new-in-l1056.html"
        },
        "results_wanted": {
            "title": "Results Wanted",
            "type": "integer",
            "description": "Maximum number of products to collect.",
            "minimum": 1,
            "default": 20,
            "prefill": 20,
            "editor": "number"
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings. Residential proxies recommended for Zara.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true,
                "apifyProxyGroups": [
                    "RESIDENTIAL"
                ]
            }
        }
    },
    "required": ["startUrl"],
    "additionalProperties": false
}